#!/bin/bash

# auto-fix the PHP code
bin/cli vendor/squizlabs/php_codesniffer/bin/phpcbf --standard=Magento2 --ignore=vendor app/code/Komoju/ 

# auto-fix the ESLint code
bin/cli eslint -c .eslintrc app/code/Komoju/ --ignore-pattern vendor/ --fix